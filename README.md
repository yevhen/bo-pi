# bo-pi

Pi extensions for tool preflight approvals.

## Usage

Install the package and let pi auto-discover extensions, or point pi at the local path:

```bash
pi install /absolute/path/to/bo-pi
```

Extensions are loaded from `./extensions` per the `pi` manifest in `package.json`.
